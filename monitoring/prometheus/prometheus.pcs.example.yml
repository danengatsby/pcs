global:
  scrape_interval: 15s
  evaluation_interval: 30s
  scrape_timeout: 10s

rule_files:
  - /etc/prometheus/rules/alerts.pcs.yml

scrape_configs:
  - job_name: pcs-api
    scheme: https
    metrics_path: /api/metrics
    authorization:
      type: Bearer
      credentials_file: /etc/prometheus/secrets/pcs_metrics_bearer_token
    static_configs:
      - targets:
          - api.example.org
        labels:
          service: pcs-api
          environment: production
